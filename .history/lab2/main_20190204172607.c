#include <stdio.h>
#include <dirent.h>
#include <sys/types.h>

int main() {
    DIR *dir;
    dir = opendir(path);
    struct dirent *dp;

    if ((dir = opendir (path) == NULL)
        printf (“opendir () Error”;

    else {
        printf (“Content of the root:\n”
        int closedir (DIR *dir);

    }
}